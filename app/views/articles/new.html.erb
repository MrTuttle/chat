<h1>New article</h1>

<%= form_with(model: [ @article.room, @article ]) do | form | %>
  <div class="field">
    <%= form.text_field :content %>
    <%= form.submit "send" %>
  </div>
<% end %>
<%= link_to 'Back', @article.room %>
