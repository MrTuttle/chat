<p style="color: green"><%= notice %></p>

<h1>Rooms index</h1>
<turbo_stream action="toast" message="Hello"></turbo_stream>

<div id="rooms">
  <% @rooms.each do |room| %>
    <%= render room %>
    <p>
      <%= link_to "Show this room", room %>
    </p>
  <% end %>
</div>

<%= link_to "New room", new_room_path %>
